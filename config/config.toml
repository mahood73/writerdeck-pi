[paths]
# Installer rewrites this template path to the selected target user's home.
root = "/home/writer/Sync/WriterDeck"
default_project = "inbox"

[editor]
command = "wordgrinder"

[sync]
folder_id = "writing"
mode = "single_writer"
wait_timeout_sec = 180
